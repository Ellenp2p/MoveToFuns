---
export const getStaticPaths = () =>
  Object.keys({
    sui101: "sui101",
  }).map((course) => ({ params: { course } }));

const { course } = Astro.params;
---

<meta http-equiv="refresh" content={`0;url=/${course}/getting-started/`} />
